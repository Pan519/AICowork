name: Build and Release

on:
  push:
    tags:
      - 'v*'  # 只在打标签时触发

permissions:
  contents: write  # 用于上传发布资产

jobs:
  build:
    timeout-minutes: 60
    strategy:
      matrix:
        include:
          # macOS ARM64 (Apple Silicon)
          - os: macos-14
            arch: arm64
            platform: mac
            build-arch: arm64
            artifact-suffix: arm64
            electron-arch: arm64

          # macOS Intel (x64)
          - os: macos-13
            arch: x64
            platform: mac
            build-arch: x64
            artifact-suffix: x64
            electron-arch: x64

          # Windows x64
          - os: windows-latest
            arch: x64
            platform: win
            build-arch: x64
            artifact-suffix: win-x64
            electron-arch: x64

          # Linux x64
          - os: ubuntu-22.04
            arch: x64
            platform: linux
            build-arch: x64
            artifact-suffix: linux-x64
            electron-arch: x64

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          timeout-minutes: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 9

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ~/.pnpm-store
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Download vendor dependencies
        run: node scripts/download-vendor-deps.js
        # 注意：只下载当前系统架构的依赖

      - name: Fix vendor executables (macOS)
        if: matrix.platform == 'mac'
        run: node scripts/fix-vendor-executables.js
        # 修复可执行文件权限

      - name: Build application
        run: |
          echo "Building for ${{ matrix.platform }}-${{ matrix.arch }}"
          pnpm run transpile:electron
          pnpm run vite:build

      - name: Build distributable
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # macOS 签名配置（如果设置了 secrets）
          CSC_NAME: ${{ secrets.CSC_NAME }}
          CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}
          CSC_LINK: ${{ secrets.CSC_LINK }}
          CSC_KEYCHAIN: ${{ secrets.CSC_KEYCHAIN }}
          CSC_KEYCHAIN_PASSWORD: ${{ secrets.CSC_KEYCHAIN_PASSWORD }}

          # Windows 签名配置（如果设置了 secrets）
          WIN_CSC_LINK: ${{ secrets.WIN_CSC_LINK }}
          WIN_CSC_KEY_PASSWORD: ${{ secrets.WIN_CSC_KEY_PASSWORD }}
        run: |
          if [ "${{ matrix.platform }}" = "mac" ]; then
            pnpm run dist:mac-${{ matrix.build-arch }}
          elif [ "${{ matrix.platform }}" = "win" ]; then
            pnpm run dist:win
          elif [ "${{ matrix.platform }}" = "linux" ]; then
            pnpm run dist:linux
          fi

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: electron-app-${{ matrix.artifact-suffix }}
          path: |
            dist/*.dmg
            dist/*.exe
            dist/*.AppImage
            dist/*.deb
            dist/*.rpm
            dist/*.zip
          retention-days: 30

      - name: Upload to GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v1
        with:
          files: |
            dist/*.dmg
            dist/*.exe
            dist/*.AppImage
            dist/*.deb
            dist/*.rpm
            dist/*.zip
          generate_release_notes: true
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
